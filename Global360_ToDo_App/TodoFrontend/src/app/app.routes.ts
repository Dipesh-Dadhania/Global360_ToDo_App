import { Routes } from '@angular/router';
import { TodoPageComponent } from './todos/feature/todo-page.component';

export const routes: Routes = [
	{
		path: '',
		component: TodoPageComponent,
	},
];
